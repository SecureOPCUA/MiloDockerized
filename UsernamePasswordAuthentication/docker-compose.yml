version: "3"
services:
  client:
    image: opcua-client
    depends_on:
      - "server"
       
  server:
    image: opcua-server
    ports:
      - "4840:4840"
    volumes:
       - ./server/credentials:/app/credentials
       
volumes:
    credentials: